

sudo docker run -it blog_web /bin/bash ??//??// get into container

sudo docker build .

sudo docker-compose up

sudo docker tag 019b21aa8d25 anonymous203030/blogging:first
sudo docker push anonymous203030/blogging

sudo docker exec -it blog_web_1  python3 blog/manage.py migrate 	//how to execute any command in docker container


sudo docker-compose up --build	//rebuild image!TO make changes in image!


sudo docker image rm "image id"
sudo docker container rm "container id"
docker image prune // remove unused


| Dockerfile  |
---------------
FROM python:3 // the version of Python
RUN mkdir /BlogAPI  // create a directory BlogAPI
WORKDIR /BlogAPI   // make it work
COPY . /blog	//copy from blog directory
RUN pip3 install -r requirements.txt	// running a command

----------------------
| docker-compose.yml |
----------------------
services:
	web:
		build: .
		command: python3 blog/manage.py runserver 0.0.0.0:8000
		volumes:
		_ .:blog
		ports:
		- "8000:8000"


